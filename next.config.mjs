/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "e-commerce-by-mithu.oneentry.cloud",
      },
    ],
  },
};

export default nextConfig;
